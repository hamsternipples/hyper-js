
import { calc_heliocentric } from './calc'

export default (T) => {
  throw '@Incomplete'
  return calc_heliocentric(T,
    x0, x1, x2, x3, x4, x5,
    y0, y1, y2, y3, y4, y5,
    z0, z1, z2, z3, z4, z5,
  )
}
